<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.traincompany.kamil.runnnn.viewmodel.summary.SummaryViewModel" />
    </data>

    <RelativeLayout
        android:id="@+id/activity_summary"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.traincompany.kamil.runnnn.view.activities.SummaryActivity">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="SUMMARY"
            android:textSize="40dp"
            android:textStyle="bold" />

        <Button
            android:layout_width="256dp"
            android:layout_height="64dp"
            android:textAllCaps="true"
            android:text="@string/doneButton"
            android:onClick="@{viewModel.onDoneButtonClicked}"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true" />

    </RelativeLayout>

</layout>